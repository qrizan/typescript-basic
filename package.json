{
  "name": "typescript-basic",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "tsc": "rm -rf build/ && tsc",
    "ts": "rm -rf build/ && tsc -w",
    "dev": "nodemon ./build/src/module/Catalog.js"
  },
  "devDependencies": {
    "nodemon": "^2.0.2",
    "typescript": "^3.8.2"
  }
}
